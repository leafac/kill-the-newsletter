exim_user = root

tls_certificate = /home/leafac/.caddy/acme/acme-v02.api.letsencrypt.org/sites/kill-the-newsletter.com/kill-the-newsletter.com.crt
tls_privatekey = /home/leafac/.caddy/acme/acme-v02.api.letsencrypt.org/sites/kill-the-newsletter.com/kill-the-newsletter.com.key

acl_smtp_rcpt = acl_check_rcpt

begin acl
acl_check_rcpt:
  accept domains = kill-the-newsletter.com

begin routers
kill_the_newsletter:
  driver = accept
  domains = kill-the-newsletter.com
  transport = kill_the_newsletter

begin transports
kill_the_newsletter:
  driver = pipe
  delivery_date_add
  envelope_to_add
  return_path_add
  temp_errors = *
  user = leafac
  path = /home/leafac/.rbenv/shims:/home/leafac/.rbenv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  environment = RAILS_ENV=production:KILL_THE_NEWSLETTER_HTTPS=true:KILL_THE_NEWSLETTER_HOST=www.kill-the-newsletter.com:KILL_THE_NEWSLETTER_EMAIL_HOST=kill-the-newsletter.com
  current_directory = /home/leafac/kill-the-newsletter
  command = /home/leafac/kill-the-newsletter/bin/rails runner 'InboxMailer.receive(STDIN.read)'

begin retry
* * F,7d,15m
