on: push
jobs:
  build:
    strategy:
      matrix:
        os: [windows, macos, ubuntu]
        include:
          - os: windows
            build: npx caxa --directory . --command "{{caxa}}/node_modules/.bin/node" "{{caxa}}/index.js" --output "kill-the-newsletter--migration--windows--${{ github.sha }}.exe"
            artifact: kill-the-newsletter--migration--windows--${{ github.sha }}.exe
          - os: macos
            build: |
              npx caxa --directory . --command "{{caxa}}/node_modules/.bin/node" "{{caxa}}/index.js" --output "kill-the-newsletter--migration"
              tar -czf "kill-the-newsletter--migration--macos--${{ github.sha }}.tgz" "kill-the-newsletter--migration"
            artifact: kill-the-newsletter--migration--macos--${{ github.sha }}.tgz
          - os: ubuntu
            build: |
              npx caxa --directory . --command "{{caxa}}/node_modules/.bin/node" "{{caxa}}/index.js" --output "kill-the-newsletter--migration"
              tar -czf "kill-the-newsletter--migration--linux--${{ github.sha }}.tgz" "kill-the-newsletter--migration"
            artifact: kill-the-newsletter--migration--linux--${{ github.sha }}.tgz
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 15
      - run: |
          npm ci
          ${{ matrix.build }}
      - uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}
