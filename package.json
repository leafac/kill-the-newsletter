{
  "scripts": {
    "prepare": "tsc && build",
    "start": "nodemon --watch \"./package.json\" --watch \"./tsconfig.json\" --watch \"./source/**/*.mts\" --watch \"./configuration/**/*.mjs\" --ext \"*\" --exec \"npm run prepare && node ./build/index.mjs ./configuration/development.mjs\"",
    "test": "npm run prepare && prettier --check \"./README.md\" --check \"./CHANGELOG.md\" --check \"./package.json\" --check \"./tsconfig.json\" --check \"./source/**/*.mts\" --check \"./configuration/**/*.mjs\""
  },
  "dependencies": {
    "@radically-straightforward/production": "^1.0.8"
  },
  "devDependencies": {
    "@fontsource-variable/public-sans": "^5.0.19",
    "@radically-straightforward/development": "^1.0.2",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  },
  "prettier": {}
}
